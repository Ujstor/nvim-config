## Install

```bash
curl -sSL https://raw.githubusercontent.com/Ujstor/nvim-config/master/install.sh | bash
```

## Update

After updating Neovim or pulling config changes, sync plugins and parsers:

```
:Lazy sync
:TSUpdate
```
